# REIST font assets

`reist-vga.psf` is generated from REIST's existing IBM-PC 8x16 kernel font by
`scripts/generate_psf2_font.py`.

`reist-unicode.psf` is generated by `scripts/generate_unifont_psf2.py` from
the vendored official GNU Unifont 16.0.04 all-plane HEX release. The exact
source archive is `source/unifont_all-16.0.04.hex.gz`, downloaded from:

https://unifoundry.com/pub/unifont/unifont-16.0.04/font-builds/unifont_all-16.0.04.hex.gz

Its pinned SHA-256 is
`20e8b505f602488697979eefc69857f7f6106bceab702f5ac559f4f84e0e7494`.
GNU Unifont 16.0.04 is distributed here under the SIL Open Font License 1.1;
the complete license text, with normalized whitespace, is stored at
`source/OFL-1.1.txt`.

The source contains 126,086 unique ordered mappings: 60,518 in the BMP and
65,568 in supplementary planes. The REIST derivative preserves native 8x16
rows. For the current fixed desktop cell, each 16x16 source row is reduced to
eight pixels by OR-ing adjacent columns. This intentionally prioritizes broad,
bounded standalone-glyph coverage over typographic fidelity. Valid Unicode
scalars absent from the source retain the visible U+25A0 fallback. This is not
a shaping, bidirectional-layout or grapheme engine.

`unicode.txt` is installed as `/usr/share/fonts/unicode.txt` as a visual
coverage sample. It uses real BMP and supplementary-plane text and labels the
still-open combining, bidirectional, shaping and grapheme behavior explicitly.

## Selectable editor families

`catalog.toml` pins five redistributable monospaced families and every source,
license and generated-runtime hash. GNU Unifont is the complete deterministic
fallback. JetBrains Mono 2.304, Source Code Pro 2.042, Iosevka 34.8.0 and Fira
Code 6.2 contribute regular-face ASCII editor glyphs plus a visible fallback
cell. Their exact upstream outline files and family-specific OFL texts are
stored below `source/`; no target process parses those TTF or OTF files.

`scripts/generate_psf2_font.py` rasterizes the four outline faces into small
24-pixel-high PSF2 subsets using the generator versions recorded in the
catalog. The target scales only those validated bitmap glyphs to the supported
10, 12, 14, 16, 18, 20, 24 and 28 pixel heights. A scalar absent from the
selected subset is looked up in GNU Unifont. OpenType ligatures, shaping and
hinting are intentionally outside this bitmap editor contract.
